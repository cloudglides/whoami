# PLACEHOLDER — this file is a sops-encrypted yaml created at bootstrap:
#   sops infra/nix/secrets/prod.enc.yaml
# (requires .sops.yaml with the real age key + sops installed locally)
# Structure documented in infra/BOOTSTRAP.md. Never commit plaintext secrets.
whoami_env: "ENC[AES256_GCM,...PLACEHOLDER...]"
glitchtip_env: "ENC[AES256_GCM,...PLACEHOLDER...]"
metabase_env: "ENC[AES256_GCM,...PLACEHOLDER...]"
